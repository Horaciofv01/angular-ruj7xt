import { CesiumTestDirective } from './cesium-test.directive';

describe('CesiumTestDirective', () => {
  it('should create an instance', () => {
    const directive = new CesiumTestDirective();
    expect(directive).toBeTruthy();
  });
});
